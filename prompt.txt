You are running an end-to-end tool integration test in /Users/tyler/workspace/jarvis; use todo_list first to track every step and mark completion as you go.
   1) Use glob to list `src/tools/*.ts`; then use read on `src/tools/index.ts` and use read_file on `AGENTS.md` (compatibility check).
   2) Use grep to find `timeout` usage under `src/tools`.
   3) Intentionally call edit on `/Users/tyler/workspace/jarvis/.tmp-tools/sample.txt` before reading it (expect read-first failure), then create it with write, read it, and edit it successfully.
   4) Use write to create `/Users/tyler/workspace/jarvis/.tmp-tools/notes.txt` with 3 lines, then use read with offset/limit to return only line 2.
   5) Use shell to run `node --experimental-strip-types --test src/tools/core-tools.test.ts` and report pass/fail.
   6) Use web_fetch on `https://example.com` with format `markdown`, summarize key content, and write the summary to `/Users/tyler/workspace/jarvis/.tmp-tools/web-summary.md`.
   7) Use ask_user with two options for cleanup (`Keep temp files` vs `Delete temp files`) and continue based on the selected option.
   8) Use sub_agent with `subagent_type: "explore"` to request a short suggestion for the next high-value tool improvement.
   9) End with a compact audit table of each tool call, whether it succeeded, and any safeguard behavior observed.
